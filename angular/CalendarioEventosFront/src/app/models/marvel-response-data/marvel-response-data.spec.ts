import { MarvelResponseData } from './marvel-response-data';

describe('MarvelResponseData', () => {
  it('should create an instance', () => {
    expect(new MarvelResponseData()).toBeTruthy();
  });
});
