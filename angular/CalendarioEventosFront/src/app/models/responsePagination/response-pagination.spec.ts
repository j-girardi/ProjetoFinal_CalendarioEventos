import { ResponsePagination } from './response-pagination';

describe('ResponsePagination', () => {
  it('should create an instance', () => {
    expect(new ResponsePagination()).toBeTruthy();
  });
});
